import style9 from 'style9';

export const { ...layout } = style9.create({
  center: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center'
  }
});
